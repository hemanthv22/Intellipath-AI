# --- Database ---
DB_USER=postgres
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Intellipath
# --- JWT ---
JWT_SECRET_KEY=change_this_to_a_random_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# --- AI ---
GROQ_API_KEY=your_groq_api_key

# --- Email / SMTP (used for the Forgot Password OTP emails) ---
# If SMTP_USERNAME/SMTP_PASSWORD are left blank, OTPs are printed to the
# backend console instead of emailed, so you can still test the flow locally.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# For Gmail, use an "App Password", not your normal account password:
# https://myaccount.google.com/apppasswords
SMTP_USERNAME=hemanthv.2468@gmail.com
SMTP_PASSWORD=dtatefsuztfoivdz
SMTP_FROM_EMAIL=hemanthv.2468@gmail
SMTP_FROM_NAME=IntelliPath

# --- OTP ---
OTP_EXPIRE_MINUTES=10
